<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical">

    <!-- Main content area -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:orientation="vertical">

        <!-- Hero Header -->
        <FrameLayout
            android:id="@+id/hero_header"
            android:layout_width="match_parent"
            android:layout_height="356dp"
            android:background="@drawable/hero_gradient_background"
            android:fitsSystemWindows="true">

            <!-- Back button -->
            <ImageButton
                android:id="@+id/btn_back"
                android:layout_width="32dp"
                android:layout_height="32dp"
                android:layout_gravity="top|start"
                android:layout_marginStart="16dp"
                android:layout_marginTop="16dp"
                android:background="@drawable/bg_back_button_circle"
                android:contentDescription="Back"
                android:padding="4dp"
                android:scaleType="centerInside"
                android:src="@drawable/ic_back_arrow" />

            <!-- Title + Subtitle area -->
            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_gravity="bottom"
                android:orientation="vertical"
                android:paddingStart="24dp"
                android:paddingEnd="24dp"
                android:paddingBottom="72dp">

                <TextView
                    android:id="@+id/hero_category_name"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:fontFamily="sans-serif-medium"
                    android:textColor="#FFFFFF"
                    android:textSize="45sp" />

                <TextView
                    android:id="@+id/hero_subtitle"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:layout_marginTop="4dp"
                    android:alpha="0.8"
                    android:textColor="@color/colorInverseOnSurfaceNew"
                    android:textSize="12sp" />

            </LinearLayout>

            <!-- Action Chips: horizontally scrollable, third chip peeks off-screen -->
            <HorizontalScrollView
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_gravity="bottom"
                android:layout_marginBottom="16dp"
                android:clipToPadding="false"
                android:overScrollMode="never"
                android:paddingStart="16dp"
                android:paddingEnd="0dp"
                android:requiresFadingEdge="none"
                android:scrollbars="none">

                <LinearLayout
                    android:id="@+id/action_chips"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:orientation="horizontal"
                    android:paddingEnd="16dp">

                    <com.google.android.material.button.MaterialButton
                        android:id="@+id/chip_add_card"
                        style="@style/Widget.Material3.Button.ElevatedButton"
                        android:layout_width="wrap_content"
                        android:layout_height="32dp"
                        android:layout_marginEnd="8dp"
                        android:insetTop="0dp"
                        android:insetBottom="0dp"
                        android:minWidth="0dp"
                        android:minHeight="0dp"
                        android:paddingHorizontal="12dp"
                        android:text="@string/action_add_new_card"
                        android:textColor="?attr/colorOnSurface"
                        android:textSize="12sp"
                        app:backgroundTint="?attr/colorSurfaceContainerHigh"
                        app:cornerRadius="8dp"
                        app:elevation="2dp"
                        app:icon="@drawable/ic_add_card"
                        app:iconSize="18dp"
                        app:iconTint="?attr/colorOnSurface" />

                    <com.google.android.material.button.MaterialButton
                        android:id="@+id/chip_start_review"
                        style="@style/Widget.Material3.Button.ElevatedButton"
                        android:layout_width="wrap_content"
                        android:layout_height="32dp"
                        android:layout_marginEnd="8dp"
                        android:insetTop="0dp"
                        android:insetBottom="0dp"
                        android:minWidth="0dp"
                        android:minHeight="0dp"
                        android:paddingHorizontal="12dp"
                        android:text="@string/action_start_review"
                        android:textColor="?attr/colorOnSurface"
                        android:textSize="12sp"
                        app:backgroundTint="?attr/colorSurfaceContainerHigh"
                        app:cornerRadius="8dp"
                        app:elevation="2dp"
                        app:icon="@drawable/ic_start_review"
                        app:iconSize="18dp"
                        app:iconTint="?attr/colorOnSurface" />

                    <com.google.android.material.button.MaterialButton
                        android:id="@+id/chip_edit_category"
                        style="@style/Widget.Material3.Button.ElevatedButton"
                        android:layout_width="wrap_content"
                        android:layout_height="32dp"
                        android:insetTop="0dp"
                        android:insetBottom="0dp"
                        android:minWidth="0dp"
                        android:minHeight="0dp"
                        android:paddingHorizontal="12dp"
                        android:text="@string/action_edit_category"
                        android:textColor="?attr/colorOnSurface"
                        android:textSize="12sp"
                        app:backgroundTint="?attr/colorSurfaceContainerHigh"
                        app:cornerRadius="8dp"
                        app:elevation="2dp"
                        app:icon="@drawable/ic_pencil"
                        app:iconSize="18dp"
                        app:iconTint="?attr/colorOnSurface" />

                </LinearLayout>

            </HorizontalScrollView>

        </FrameLayout>

        <!-- Flashcard List -->
        <FrameLayout
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:layout_weight="1">

            <androidx.recyclerview.widget.RecyclerView
                android:id="@+id/listview_flashcard"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:clipToPadding="false"
                android:paddingBottom="8dp" />

            <TextView
                android:id="@+id/empty_category_text"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:gravity="center"
                android:text="@string/flashcard_empty_text"
                android:textColor="?attr/colorOnSurfaceVariant"
                android:textSize="16sp"
                android:visibility="gone" />

        </FrameLayout>

    </LinearLayout>

</LinearLayout>
